fun main : () -> u8 = {
    let x: u32 = 100;
    
    if(10 > 0) {
        @print(1);
    } 

    if (0 > 10) {
        @print(2);
    }

    if(x > 32) {
        @print(3);
    } else {
        @print(4);
    }

    if(10 < 0) {
        @print(5);
    } else {
        @print(6);
    }

    if(1 == 1) {
        if(2 != 1) {
            @print(7);
        } else {
            @print(8);
        }
    } else {
        @print(9);
    }

    return 0;
};