
fun main : () -> u8 = {

    @print(test(33));
    return 0;
};

fun test : (param1: u32) -> u32 = {
    return another_test(param1) * param1;
};

fun another_test : (x: u32) -> u32 = {
    return x + 33;
};